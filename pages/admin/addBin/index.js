import React from 'react';
import {Box} from '@material-ui/core';

const AddBin = () => {
    return (
        <div>
            <Box height={'200vh'}>
                Add bin
            </Box>
        </div>
    );
};

export default AddBin;
