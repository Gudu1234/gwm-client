import React from 'react';
import {Box} from '@material-ui/core';

const Settings = () => {
    return (
        <div>
            <Box height={'200vh'}>
                Settings
            </Box>
        </div>
    );
};

export default Settings;
